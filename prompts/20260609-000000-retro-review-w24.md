---
session: "01BNnpDxY7WCn8AdjGAwvNT2"
timestamp: "2026-06-09T00:00:00Z"
model: claude-sonnet-4-6
---

## Human

Run the /retro-review skill for the kemper/mainifold repo. Read every entry in
retros/inbox/, cluster by theme, apply confident process changes as diffs to
CLAUDE.md / docs/ / .claude/skills/, file tooling asks as backlog items,
write a durable report to retros/reports/2026-W{ww}.md, archive inbox entries,
and open a draft PR into main titled "chore: weekly retro 2026-W24".

## Assistant

## Key decisions

**Week number:** Entries span 2026-06-05 → 2026-06-07 (ISO W23), but the
review runs on 2026-06-09 (W24), so the report is named 2026-W24. The
prior 2026-W23 report covered the Jun 4 entries (already archived).

**Highest-impact fix: sdf.md gyroid `thickness` guidance was wrong.** Two
independent agents (lattice ball + gyroid cube) followed the documented
`cellSize/6..cellSize/3` ratio and got solid blobs. The field ranges ~0–1.5,
so cellSize=10 gives thickness ≈ 1.67–3.33 — all solid. Corrected to
empirically-verified field-unit ranges (0.4–0.7 for open lattice).

**model:preview engine listing update:** PR 470 shipped voxel + scad support.
CLAUDE.md still said "manifold-js engine" only — updated to reflect reality
and explicitly document that replicad/OpenCASCADE is excluded (Node SSR
constraint).

**api.label {color} is manifold-js only:** replicad's BREP.label takes no
color argument, but colors.md and replicad.md implied parity. Added explicit
caveats to both files.

**componentCount discipline:** agents tuned parameters blindly when count was
wrong instead of decompose-and-inspect. Added a clear rule to CLAUDE.md.

**Image token compounding:** `model-sculpt` subagent pattern was newly
documented in agent-tooling.md but not surfaced in the model:preview section
that CLI agents read. Added the delegation tip there.

**Not applied (already documented):** The SCAD for-loop label drop is already
in colors.md lines 231-234; the tooling fix (promote INFO to warn) is backlog.
The new-agent-session-registration gotcha is already in agent-tooling.md
lines 25-30.
