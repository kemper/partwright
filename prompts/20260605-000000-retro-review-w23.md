---
session: "retro-review-2026-W23"
timestamp: "2026-06-05T00:00:00Z"
model: claude-sonnet-4-6
---

## Human

Run the `/retro-review` skill: read retros/inbox/, cluster by theme, apply confident
process changes as diffs to docs/, open a draft PR titled "chore: weekly retro 2026-W23".

## Assistant

### Key decisions

**Two entries, ~12 sub-agents, one dominant theme.** The catalog-quality-pass retro
(10 parallel sub-agents) and its follow-up (2 agents) both converged on the same top
friction: the catalog thumbnail camera faces iso azimuth ≈135° (the +X/−Y corner) but
this was documented nowhere. Eight agents across the two passes hit the orientation
mismatch and wasted 1–2 bakes each rebuilding models.

**Applied three doc fixes, left tooling as backlog.** The four "Longed for" items
(thumbnail-camera flag, warm bake engine, richer BAKE_RESULT stats, file-size CI gate)
all require new tooling or script changes — beyond the retro facilitator's scope. Applied
only the pure-doc fixes: orientation Gotcha in voxel.md, file-size and thumbnail-lighting
notes in colors.md, both in catalog/README.md.

**Replaced the `detail` description in voxel.md rather than appending.** The existing
line said `detail` gives "finer, more controlled rounding on coarse/small models" which
is only half-true and actively misled an agent. Changed the line to qualify "large, coarse
shapes" and added the caveat about small/dense features — this is a fix, not an addition.

**Skipped the replicad `fuseAll` + `coverageMode` cliff as a doc change.** The existing
`replicad.md` gotcha #9 already covers the label-scramble root cause, and the
`coverageMode:'fully_inside'` small-feature cliff is noted in `colors.md`'s fan-topology
bleed section. Noting in the report without duplicating.
