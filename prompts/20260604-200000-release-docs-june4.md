---
session: "ZvDxi"
timestamp: "2026-06-04T20:00:00Z"
model: claude-opus-4-8
tools: [playwright]
sequence: 1
---

## Human

Please create a new release from staging to production. Update the changelog,
help and other docs as needed based on the new features since the last deploy.

## Assistant

## Key decisions

**Split the request along the pipeline boundary.** The deployment rules in
CLAUDE.md forbid putting changelog/release-note edits onto the release branch —
the `production-promotion-guard` fails any `staging → production` PR carrying
content not already on `main`. So the doc updates land on `main` first (this
`docs:` PR on the designated branch); the actual `staging → production` release
PR is cut only *after* this merges and the gate fast-forwards `staging`.
Surfaced that sequencing to the user rather than opening a release PR that would
either trip the guard or omit the new changelog.

**Scoped "new features since last deploy" to `production..staging`.** A release
already shipped earlier today (PR #420, 12:43). Diffing that range surfaced ~40
non-merge feature commits whose marquee items were missing from the changelog:
the fabric/knit surface-texture family (knit/cable/waffle/fur/woven, WebGPU
displacement, LSCM/harmonic UV), the click-to-select region selector now on all
surface modifiers, the catalog quality pass, worker-health diagnostics, the
viewport reset-view + zoom limit, color-bucket fixes, and a batch of
reliability fixes (Worker recovery, draft clearing, per-part state).

**Did NOT touch the AI docs.** One commit said "expose texture tools to AI" — I
verified `public/ai/textures.md` and the `applyKnitTexture`/`applyCableKnit`/etc.
tool schemas in `src/ai/tools.ts` already document the full texture surface, so
no ai.md/subdoc edits were needed. Left `llms.txt` alone too — it enumerates
engines and the console API, not viewport tools.

**Edits.** Added a `June 4, 2026` entry to `whatsNew.ts` (used the release date
rather than overlapping the existing May 30 / May 31–June 3 ranges) and expanded
the Help page's Surface viewport-tool bullet to cover the fabric textures and
region targeting, plus a new reset-view bullet. Verified with build + unit (640
pass) and a screenshot of the static `/whats-new` page.
