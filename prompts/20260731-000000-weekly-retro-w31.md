---
session: "retro-review-w31"
timestamp: "2026-07-31T00:00:00Z"
model: claude-sonnet-5
tools: [retro-review-skill]
---

## Human

[Scheduled task] Run the /retro-review skill for kemper/mainifold: read
retros/inbox/, cluster by theme weighting by distinct-session frequency,
apply confident process diffs to CLAUDE.md/docs/skills, backlog anything
needing new tooling, write retros/reports/2026-W31.md, archive processed
entries, and open a draft PR into main titled "chore: weekly retro
2026-W31" with the ignore-for-release label.

## Assistant

## Key decisions

**Scope:** 12 inbox entries spanning three work clusters — the inverse-CAD
v2 rebuild/convergence run (4 entries), editor/AI features (multi-part
assembly, tool-history repair, mesh→code, part grouping — 4 entries), and
Blender-parity mesh-shaping verbs + their 16-agent catalog rollout (2
entries, one an aggregate). Confirmed the most recent prior report was
W27, so no gap in coverage and no risk of double-processing.

**Applied now (doc-only, high-confidence):**
- `public/ai/deform.md` — a worked numeric example for sizing `scatter`'s
  `offset` against instance thickness, and a `radius ≈ 4–6% of edge length`
  rule of thumb for a machined `round` look. Both raised by 3 independent
  agents in the catalog-refresh batch; chose these over the `deform.md`
  edits the batch's own PR already made (round-first-then-paint,
  thin-shell caution) which were confirmed already present.
- `docs/playwright-guide.md` — added a gotcha for rail/list drag specs
  needing a taller viewport than the 1280×900 default. Only one session
  raised it, but it's precise, cheap, and directly prevents a repeat of a
  concretely wasted debugging loop — judged worth the exception to the
  "don't harden one-offs" default.

**Backlogged, not applied (needs new tooling or code):** an inverse-CAD
convergence driver/status script (3 independent sessions converged on this
from three different failure angles — a part silently never assigned, a
stale gate-passing "best," and ambiguity between "agent running" and "best
exists"); a warm browser loop for UI-interaction verification (distinct
from the existing model-code render daemon); the already-tracked `#928`
scatter-placement-feedback ask, now quadruple-voted; region-scoped
`smoothWeld`/`round`; a label-exposure/off-camera-label signal for
`model:preview`; and several single-agent asks (one-shot bake script,
build-plate packing visualizer, readiness-poll test helper, `placeOnFace`,
genus-change flag).

**Left as one-offs, no action:** several items turned out to already be
documented from prior retro cycles (send_later limitation, capability
registry drift, promptlog-guard sequencing) — the reporting sessions
simply predated those additions. Tool-history's four-implementation
fragmentation is already tracked as issue `#914`, so no new doc action.

**Process note:** did not touch `.claude/agents/*` or hook/settings.json
per the skill's scope boundary — none of this cycle's asks required it.
