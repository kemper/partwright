---
session: "inverse-cad"
timestamp: "2026-07-03T18:30:00Z"
model: claude-fable-5
sequence: 23
---

## Human

(continuation — v2 re-convergence waves)

## Assistant

## Key decisions

**12/21 converged all-MUST-green.** New this round:
- hand_open_left (v1's WORST part, 1.37 plateau): ONE authored turn,
  chamfer 0.024 — pure recipe transfer, only the pose-specific ledge list
  re-measured (§5.13a: a true ledge is one giant Δ in a 0.05mm area scan;
  consistent small Δs are dome curvature).
- frame_hips (the part whose v1 inversion was "a complete disaster"):
  chamfer 0.0005, one attempt. 3 balls r=3.000 at z=0 + D-section strut;
  bbox-center z=0.25 is the asymmetric clip, NOT the feature line — probe
  centers, never infer from bbox.
- hand_fist_left: chamfer 0.0216, 3 turns, including binary-searching
  partial band stacks to localize a fabricated genus handle and fixing it
  with 0.2mm pitch over just the knuckle region (§5.19a/b).

Ratchets applied for all three. Launched: hand_open_right + hand_fist_right
(mirror shortcuts) + frame_head (worst bootstrap, IoU 0.532). Running:
knee_elbow + these three.
