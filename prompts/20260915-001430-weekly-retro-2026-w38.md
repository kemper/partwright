---
session: "retroreview38"
timestamp: "2026-09-15T00:14:30Z"
model: claude-sonnet-5
tools: [retro-review-skill]
files_changed:
  - CLAUDE.md
  - docs/architecture-notes.md
  - docs/playwright-guide.md
  - retros/reports/2026-W38.md
  - retros/archive/2026-W38/*
---

## Human

[Scheduled trigger] Run the `/retro-review` skill for the `kemper/mainifold`
repo: read `retros/inbox/`, cluster entries by theme weighted by distinct
sessions/agents, apply confident process diffs to CLAUDE.md/docs/skills,
file anything needing new tooling or agent/hook changes as backlog, write a
durable report to `retros/reports/{YYYY}-W{ww}.md`, archive the processed
inbox entries, and open a draft PR into `main` titled
`chore: weekly retro {YYYY}-W{ww}` with the `ignore-for-release` label and
the report as the PR body. Then stop — a human merges it.

## Assistant

## Key decisions

**Scope of the inbox this cycle:** 12 entries (2026-07-06 through
2026-07-20), split across two clusters — app/UI feature work (assembly grid
view, AI tool-history repair, 3MF/Bambu packing) and the inverse-CAD
reconstruction framework (v2 rebuild, armor genericity phase, first foreign
mesh, catalog-bake tooling). One entry (catalog-refresh-agent-feedback)
pre-aggregates structured feedback from 16 `model-sculpt` sub-agents with its
own vote counts, which I treated as already-triaged input rather than
re-deriving frequency myself.

**Cross-checked against retros/reports/2026-W23–W27** before proposing any
CLAUDE.md/doc edit, specifically to (a) avoid re-litigating items already
fixed (`send_later` unavailability, the capability-registry deferral,
promptlog-guard sequencing, `model:preview --silent` — all re-raised this
batch but already documented, so logged as "already resolved" with no new
diff) and (b) check whether the "warm browser for UI iteration" wish (raised
twice this batch) had prior votes — it hadn't, so it's a new backlog item
rather than an escalation.

**Applied only 3 direct edits**, each either (a) tightening an existing rule
with new specificity rather than adding a new one, or (b) a single concrete,
low-risk factual addition to a docs file whose whole purpose is exactly this
kind of gotcha:
1. `CLAUDE.md`'s NUL-byte `main.ts` callout said "use `grep -a` or `rg -a`"
   without saying *which* tool — a session burned a call passing `-a` to the
   harness's dedicated `Grep` tool (no such flag exists there) before
   falling back to `Bash`. Replaced the ambiguous line rather than appending
   a second one.
2. `docs/playwright-guide.md`: added a numbered gotcha that rail/list-drag
   e2e specs silently miss drop targets below the fold on the default
   viewport height — one session lost two runs to this.
3. `docs/architecture-notes.md`: added a new "Worker client handshake
   protocol" section (init → ready → error) after a pooled Worker client
   hung silently for lack of it. Placed next to the existing lazy-WASM-load
   section since it's the same "new async subsystem" audience.

**Left three multi-vote clusters as backlog rather than agent/hook edits**,
per the skill's scope boundary (tooling/new scripts and `.claude/agents/*`
changes are out of direct-edit scope even with strong signal): a
warm-browser UI-iteration harness (2 votes, new this cycle), inverse-CAD
orchestration state-tracking (3 votes across 3 different failure symptoms —
stale "best" after a gate change, an unassigned part, consuming an interim
result mid-run — which I clustered together as one root cause: no
status/state file), and a three-way component/genus counter for degenerate
meshes (2 votes). Also declined to guess at a "main.ts module-scope vs
setup-scope" doc addition requested by one session — writing an accurate
rule needs more file-specific investigation than a retro pass should do
blind, so it's noted as a one-off pending a second vote or a dedicated pass.

**Archive + report:** moved all 12 processed entries into
`retros/archive/2026-W38/`, wrote `retros/reports/2026-W38.md` following the
skill's template, then branched `chore/weekly-retro-2026-w38` from
`origin/main`, committed, pushed, and opened the PR as a draft with
`ignore-for-release` per the skill's closing step.
