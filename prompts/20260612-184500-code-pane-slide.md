---
session: "elegant-clarke-25f7wu"
timestamp: "2026-06-12T18:45:00Z"
model: claude-opus-4-8
tools: [playwright]
sequence: 4
---

## Human

Let's make it slightly less fast and apply the same slide behavior to opening
and hiding the code pane.

## Assistant

## Key decisions

**Shared, slightly-slower duration.** Renamed `appConfig.ui.aiPanelSlideMs` →
`paneSlideMs` (the field was new in this same PR, so no released override to
migrate) and bumped the default 200 → 280ms. Both the AI panel and the code pane
now read this one knob, so the two side panes feel consistent. Advanced Settings
field relabeled "Pane slide".

**Code pane uses the AI panel's exact technique, mirrored to the left edge.** The
"Hide code" collapse previously snapped `editorGroup` width 40% → 0. It now
animates `margin-left` to `-width` so the pane slides off the *left* edge while
its layout footprint collapses and the viewport grows into the gap — content
keeps its width (no CodeMirror reflow), and `body`'s `overflow-hidden` clips the
off-screen pane. After the slide it settles to the canonical `width:0` collapsed
state; expand reverses it (start off-screen-left without transition, then slide
in). Width stays out of the transition so splitter-drag resizing isn't animated.

**Reduced-motion + breakpoint safety.** Honors `prefers-reduced-motion` (instant)
and only animates on desktop (`mqDesktop`); the mobile stacked layout stays
instant. `syncPaneVisibility`'s mobile branch now also clears any in-flight
`margin-left`/`transition` so a breakpoint flip mid-slide can't strand the pane
off-screen. Boot stays instant (the initial collapsed state is applied directly,
not through `collapseEditor`).

**Verification.** Slowed the slide to 1200ms and captured mid-close / mid-open
frames showing the code sliding off (and back onto) the left edge with the
viewport growing/shrinking. The e2e that toggle the code pane (smoke "Show code",
customizer collapse-during-drag) still pass — Playwright treats the off-screen-
but-sized pane as visible, and the terminal states are unchanged.
