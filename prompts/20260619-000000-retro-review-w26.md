---
session: "retro-w26-2026"
timestamp: "2026-06-19T00:00:00Z"
model: claude-sonnet-4-6
tools: [Read, Bash, Grep, Edit, Write]
---

## Human

Run the weekly retro-review skill: process all entries in retros/inbox/, cluster by theme, apply confident process diffs to CLAUDE.md and skills, write the 2026-W26 report, archive entries, open a draft PR.

## Assistant

## Key decisions

**36 entries spanning Jun 11–18.** W25 report already existed (covered Jun 9–11 entries), so named this one W26.

**Top theme by far: headless colored render gap (~20 agents).** `model:preview` shades by face normal and can't show label/palette colors. Nearly every figure or catalog session hits the 75s xvfb bake because there's no fast colored preview. Left as a backlog tooling item (requires new script capability, not a doc fix), but expanded the existing model:preview callout with the paint-relief-floor rule and azimuth convention clarity.

**Second theme: `build-catalog-entry.cjs` dev-server requirement undocumented (5 agents).** The "API never appeared" error means no dev server at :5173 — not an xvfb issue. Added explicitly to the callout rather than letting agents keep discovering it.

**Third theme: catalog freshness / stale bakes (5 agents).** Source-bake divergence has no CI check. Left as a backlog item; added `.thumb.png` untracked-sidecar note to reduce Stop hook noise.

**Applied diffs only for things with 2+ independent agent signals** or where a single agent pointed at a clear doc gap (bounds() conservative, azimuth convention, background bake container idle, altitude scoping question). Rejected adding rules for one-offs (Python json.dump, guess-issue-number, promptlog staging race).

**CLAUDE.md is already large; every change replaces rather than appends.** The smoothUnion/bounds() note already existed — strengthened the existing sentence rather than adding a new section. The build-catalog-entry callout already existed — expanded it in place.

**Telegram numeric channel ID** (`-1003948930577`) added to retro-review.md Reminders so future retro agents don't burn turns on `getUpdates` discovery.
