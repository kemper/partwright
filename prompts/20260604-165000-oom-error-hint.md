---
session: "8fTQp1"
timestamp: "2026-06-04T16:50:00Z"
model: claude-opus-4-8
tools: [playwright]
sequence: 2
---

## Human

Tested a model at quality 1024 Ultra and got "null function" with source:
engine and no stack trace. Attached the model (a parameterized spiral chain).
Not sure if it's related to the render-timeout removal.

## Assistant

## Key decisions

**Diagnosed first, confirmed not a regression.** Reproduced the exact error
(`null function or function signature mismatch`) with the attached model at
1024 segments. It crashed at ~31s execution time — *under* the old 60s
timeout — so the WASM aborts on its own before any timeout would have fired.
Removing the render timeout did not cause or worsen it. The model renders fine
at Medium (32 seg, 514k tris) and 256 seg (1.03M tris); only the jump to Ultra
(1024) overflows the manifold WASM heap. Root cause: a 1024-vertex
`CrossSection.circle` profile swept over ~67 points × 30 links, then unioned.

**Folded a small message fix into the timeout PR (user's call), then dropped it
when main superseded it.** The engine already enhances common WASM errors with
actionable hints (`manifoldJs.ts`), but the Emscripten OOM/abort traps weren't
covered, so they surfaced as the cryptic "null function…" with no stack. I added
a branch matching the trap signatures and verified in-browser that the hint
appended to the crash message. On the next re-sync, merging `origin/main`
brought in PR #416, which had *already* shipped a superior centralized solution:
`src/geometry/workerFaults.ts` (`isFatalWasmFault` / `isWasmMemoryFault` /
`wasmFaultHint`) — it covers the same trap signatures plus worker recycling so a
poisoned WASM instance is recycled, not reused. I resolved the merge conflict by
discarding my hand-rolled branch in favor of `wasmFaultHint(msg)`, so this PR
adds no OOM-handling code of its own. The diagnosis still stands: the 1024/Ultra
crash is a pre-existing memory fault (crashes at ~31s, before the old 60s
timeout), unrelated to the timeout removal — and now main already gives the
user a clear, memory-aware message for it.
