---
session: "8fTQp1"
timestamp: "2026-06-04T16:50:00Z"
model: claude-opus-4-8
tools: [playwright]
sequence: 2
---

## Human

Tested a model at quality 1024 Ultra and got "null function" with source:
engine and no stack trace. Attached the model (a parameterized spiral chain).
Not sure if it's related to the render-timeout removal.

## Assistant

## Key decisions

**Diagnosed first, confirmed not a regression.** Reproduced the exact error
(`null function or function signature mismatch`) with the attached model at
1024 segments. It crashed at ~31s execution time — *under* the old 60s
timeout — so the WASM aborts on its own before any timeout would have fired.
Removing the render timeout did not cause or worsen it. The model renders fine
at Medium (32 seg, 514k tris) and 256 seg (1.03M tris); only the jump to Ultra
(1024) overflows the manifold WASM heap. Root cause: a 1024-vertex
`CrossSection.circle` profile swept over ~67 points × 30 links, then unioned.

**Folded a small message fix into the timeout PR (user's call).** The engine
already enhances common WASM errors with actionable hints
(`manifoldJs.ts`), but the Emscripten OOM/abort traps weren't covered, so they
surfaced as the cryptic "null function…" with no stack. Added a branch matching
the trap signatures (`null function or function signature mismatch`,
`memory access out of bounds`, `table index is out of bounds`,
`Cannot enlarge memory`, `Out of memory`, `Aborted`) that appends a hint
telling the user to lower the quality preset or set explicit segment counts.
Thematically adjacent to the timeout removal — with the auto-kill gone, a clear
message is the remaining guidance for a too-complex render. Verified in-browser
that the hint now appends to the crash message.
