---
session: "retro-2026-W27"
timestamp: "2026-06-30T00:00:00Z"
model: claude-sonnet-4-6
tools: [subagent/explore, git]
---

## Human

Run the weekly retro-review skill for the kemper/mainifold repo: read every entry in retros/inbox/, cluster by theme, apply confident process changes as real diffs to CLAUDE.md/docs/.claude/skills/, write a durable report to retros/reports/2026-W27.md, move processed entries to retros/archive/2026-W27/, and open a draft PR into main.

## Assistant

## Key decisions

**95 entries from ~55 sessions (June 11–28, 2026) — large inbox, but themes were clear.** Used an Explore subagent to read all files in parallel and return a structured theme map. Did not read all 95 files in the main context.

**Three confident CLAUDE.md edits, all replacing stale-or-absent content:**

1. **"Flush-surface paint dithers → add relief"** — Appended to the existing `Paint-in-code is verified headlessly` bullet. ~12 sessions rediscovered that areola, eyebrow, and skin marking paint produces salt-and-pepper edges when the decorated layer is exactly flush. The tool (`stats.paintOps`) gives no warning because tri-count is non-zero; only the colored bake reveals it. Chose to extend the existing bullet rather than add a new callout, to keep the rule collocated with the related paint tooling.

2. **"Garment/accessory QC requires side + back views"** — Extended the existing multi-angle callout. ~10 sessions shipped front-only-validated accessories that only broke from the side or back (belt on arm, necklace through dress, garment back coverage hole). The existing rule named the underside; the garment/accessory pattern is the same class of defect but in a different direction.

3. **"Catalog `.partwright.json` goes stale when source changes"** — Added to the `build-catalog-entry.cjs` section. ~8 sessions discovered mid-PR that catalog geometry was stale after a source fix. No CI gate exists. Added the rule and referenced the backlog item for a freshness CI gate.

**Did NOT add rules for already-documented patterns** (SSR vs browser componentCount divergence, `bounds()` loose AABB, `model:preview` needs `--palette-file` for figure colors) — all already covered in CLAUDE.md. Adding again would be restatement.

**Backlog rather than auto-apply for 9 items** that required new tooling, new scripts, or architectural changes: headless colored render, catalog freshness CI, browser-faithful componentCount, figure invariant gate, `catalog:rebake` script, capability registry, figure-api schema, per-figure snapshot regression, Bambu GUI load validation, `send_later` self-wake. These are real, high-impact asks but can't be applied as instruction edits.
