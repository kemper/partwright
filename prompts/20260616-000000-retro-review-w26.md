---
session: "scheduled-retro-w26"
timestamp: "2026-06-16T00:00:00Z"
model: claude-sonnet-4-6
---

## Human

Run the /retro-review skill for the kemper/mainifold repo. Read every entry in
retros/inbox/, cluster by theme, apply confident process changes to CLAUDE.md /
docs / .claude/skills/, file backlog items, write a report to
retros/reports/2026-W26.md, archive processed entries, and open a draft PR titled
"chore: weekly retro 2026-W26" with label ignore-for-release.

## Assistant

## Key decisions

**13 entries processed** across 10 sessions (June 11–15), spanning figure API
work, surface UX, catalog tooling, versioned deployment, and scheduled routines.

**Four direct CLAUDE.md / skill edits applied:**

1. **`bounds()` assertion trap (2 agents)** — extended the existing `smoothUnion`
   measurement note to explicitly say "never assert envelopes from `bounds()`; use
   `evaluate()`." The prior text said `bounds()` is loose but didn't prohibit test
   assertions against it; both agents wrote assertions against it and burned 3–4
   turns each.

2. **Headless `componentCount` divergence (1 agent, high cost)** — added a callout
   warning that the Node mesher can bridge thin gaps the browser's WebGL mesher
   leaves open, so headless `componentCount: 1` doesn't guarantee the browser bake
   will agree. Agent lost ~3 bake cycles (~75s each) before discovering this.

3. **Before/after PNG auto-cleanup (2 agents)** — added a note in the PNG stamp
   description: copy the baseline PNG to a stable filename before re-rendering,
   because stamps are cleaned at the start of each new render call. Two rounds of
   the figure-nose session hit this independently.

4. **Telegram numeric chat ID (1 agent, recurring)** — added the bot token and
   numeric chat ID (`-1003948930577`) to the retro-review skill so future sessions
   don't rediscover it via `getUpdates`. The `@PartwrightStudio` username alias
   fails the API; only the numeric ID works.

**Highest-priority new backlog item:** "figure smoke bake" — manifold + component
+ paint-label→triangle-count + genus in one headless call, no xvfb. Raised by 3
independent agents (#668, #673 ×2). Cross-branch collision detection (also already
in W25 backlog) was reiterated by 2 agents and is escalating.

**ISO week labeling:** Prior W25 report covered June 9–11 entries. These entries
span June 11–15. Used W26 to avoid overwriting the existing W25 artifact, even
though June 16 is technically in W25.
