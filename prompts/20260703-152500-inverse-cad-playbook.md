---
session: "inverse-cad"
timestamp: "2026-07-03T15:25:00Z"
model: claude-fable-5
sequence: 11
---

## Human

(continuation — v2 framework build)

## Assistant

## Key decisions

**PLAYBOOK.md — the durable agent field manual** (GRAMMAR.md evolved, the
deliverable whose absence made every v1 subagent rediscover the same two
traps). Contents: the 10-line loop contract, pre-first-edit checklist
(off-center bbox, genus→subtractions, strategiesTried), the phase ladder,
the strategy decision tree, a tactics catalog (slice-and-trace, primitive
probing, the socket/cavity pattern, chamfer diagnosis, scaffold erosion,
optimizer params), the plateau protocol, the append-only API-trap list (now
including the paramsSchema-is-an-array and verify-your-patch-anchors traps
discovered this session), feedback-bundle semantics, and the full worked
ankle example with real numbers.

Written from measured experience in this session, not aspiration: every
tactic cites the actual result it produced (trace → 0.066 chamfer;
probe fit → sphere 98.6% inliers; optimizer → already-optimal verdict).
