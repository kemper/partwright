---
session: "inverse-cad"
timestamp: "2026-07-06T01:30:00Z"
model: claude-fable-5
sequence: 60
---

## Human

Wants all Dummy 13 components in ONE catalog entry (not separate frame
and armor kits). Also confused by the "parameterizable" badge showing on
the frame kit but not armor — asked whether to remove params or design
cross-part params.

## Assistant

## Key decisions

**Merged into `dummy13-complete-kit`**: one 37-part session (21 frame +
16 armor types, 58 pieces with quantities), replacing both old entries.
1253KB (under the 1500 gate), all per-part component gates exact,
composite contact-sheet tile. Multi-part bake thumbnails switched to
128px JPEG (~3KB each vs ~10KB PNG) to make room at 37 parts.

**Stripped the leftover `api.params`** from the 4 frame parts that had
them (chest, head, hip/shoulder, waist) — they were the convergence
loop's tuning knobs (socket radii etc.), not user-facing features, and
they caused the inconsistent "Customizable" badge. Defaults inlined as
constants in the entry code; the tuning ranges remain in
`scripts/inverse-cad/converged/` for future re-convergence.

**Filed #892** for the deliberate version of the user's cross-part
idea: session-level parameters (global joint clearance, whole-figure
scale) — the granularity that actually makes sense for kits, designed
rather than shipped by accident.
