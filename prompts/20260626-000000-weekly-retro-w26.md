---
session: "retro-w26-scheduled"
timestamp: "2026-06-26T00:00:00Z"
model: claude-sonnet-4-6
tools: [Explore, Grep, Read, Edit, Write, git]
---

## Human

Run the weekly `/retro-review` skill: read retros/inbox/, cluster by theme, apply confident process diffs, write report to retros/reports/2026-W26.md, archive entries, open draft PR.

## Assistant

## Key decisions

**84 entries, ~60 distinct sessions.** Span 2026-06-11 → 2026-06-23. High-signal week — the figure API landed many catalog entries, which produced a tight cluster of oracle-gap rediscoveries.

**Highest-priority diff: strengthen the "colored bake is the only oracle" callout.** At least 12 independent sessions re-discovered that `model:preview` SSR under-reports `componentCount` for marginal welds and can't validate paint-label resolution. The guidance existed as a single subordinate note; I elevated it to a named, bold statement in the `figure:smoke` callout. Also added the hair-texture edgeLength trap (`coils`/`strands` alias at edgeLength ≥ ~0.5) as a named sibling warning — 5+ sessions hit this independently with no prior documentation.

**Second diff: `smoothUnion` label-wipe trap.** ~5 sessions discovered silently that `smoothUnion(prop, figure)` wipes all label attribution from the figure. The fix is always "hard-union the prop instead" and takes one line — the trap just wasn't documented. Added a named callout to the `smoothUnion` section.

**Third diff: issue hygiene inverse.** The existing guidance said "don't close issues prematurely." The missing flip side — "do close issues when criteria ARE met" — was causing ~5 sessions to re-investigate fully fixed bugs because their source issues were left open. Added one sentence to Issue hygiene point 2.

**Fourth diff: agent working discipline — two new bullets.** "Brief triage agents on confirmed defects, not open-ended sweeps" codifies the lesson from 4 sessions where open-ended "find all defects in 43 figures" produced high false-positive rates at high cost. "Check whether a capability already exists before building it" captures the daemon/compare/colored-preview re-requests.

**Fifth diff: Duplicated Logic — grep for old-idiom copies.** When fixing `sdfFigure.ts`, examples that hand-rolled the old idiom inline don't pick up the engine fix. A one-second grep finds them; without it, figures ship with the stale anti-pattern.

**Deferred to backlog (not auto-applied):** Browser-faithful headless componentCount gate (high value, needs Chromium in test harness); per-figure visual snapshot CI (large storage); global-rig pose-coverage test; `api.sdf.figure` guard rails (engine work); catalog freshness lint; `catalog:rebake` convenience script. These require new tooling or agent changes beyond CLAUDE.md edits.
