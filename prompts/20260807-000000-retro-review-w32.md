---
session: "20df8dbe-c93e-51fe-9180-0f2d129cc384"
timestamp: "2026-08-07T00:00:00Z"
model: claude-sonnet-5
---

## Human

[Scheduled task] Run the /retro-review skill for the kemper/mainifold repo.
Read every entry in retros/inbox/, cluster by theme weighted by distinct
sessions/agents, apply confident process diffs to CLAUDE.md/docs/skills
(preferring replacing stale lines over appending), file anything needing new
tooling or `.claude/agents/`/hook changes as backlog items instead of
auto-applying, write a durable report to retros/reports/2026-W{ww}.md, move
processed entries to retros/archive/2026-W{ww}/, and open a draft PR into
main titled "chore: weekly retro 2026-W{ww}" with the `ignore-for-release`
label and the report as the PR body. Then send a Telegram summary.

## Assistant

## Key decisions

**Week number:** 12 inbox entries span 2026-07-06 → 2026-07-20; the review
runs 2026-08-07, so the report is 2026-W32 (the last report was W27 — several
weeks of entries had accumulated unprocessed).

**Highest-vote cluster — Blender-parity verb sizing/scoping (up to 4 agents
per item, from the 16-subagent catalog-refresh aggregate entry):** applied
three doc fixes directly to `public/ai/deform.md` rather than leaving them
all as backlog, since they're pure documentation and the asks were concrete:
(1) a worked `offset`-vs-instance-thickness sizing recipe under Scatter
(two agents fully buried their instances on a first pass), (2) a "radius ≈
4–6% of edge length" machined-look rule of thumb under Round, complementing
the existing thin-feature-ceiling guidance rather than replacing it, (3) a
local clip/weld/stitch recipe under smoothWeld as the documented workaround
for the whole-bbox lattice cost, until a native `region` option exists
(left as backlog — that's a code change, not a doc one).

**Inverse-CAD orchestration gap — 3 independent sessions, same root cause:**
v2 (stale gate-passing best), armor-genericity (a part silently never
assigned an agent), and benchy (baked from an unfinished interim best) all
independently hit "no status/driver layer over the multi-agent convergence
pool." Didn't build `status.mjs` myself (out of scope for a doc-only pass —
it's new tooling) but wrote it up precisely as the top backlog item since
three-way corroboration is strong signal.

**Distinguished an already-solved problem from a still-open one:** two
sessions asked for a "warm-browser UI loop," which sounds like it duplicates
the already-shipped Phase-2 `partwright daemon` (documented in
`docs/headless-cli.md`). Read that doc before writing the backlog item —
the daemon covers `window.partwright` method calls and model renders, not
arbitrary DOM screenshot/interaction, so the ask is real and distinct
(extending the daemon to DOM-level UI verification), not a duplicate.

**Not applied (one-offs / already covered):** worker init/ready-handshake
doc gap, main.ts module-scope note, showToast signature confusion (existing
CLAUDE.md prose already implies the options-object form), rail/list-drag
viewport-height note, NUL-byte grep recall miss, promptlog-guard staging
order — all single-session mentions, several confirming already-documented
rules rather than surfacing new gaps. Left as "One-offs noted," per the
skill's anti-bloat instruction not to harden a one-off into a permanent rule.

**Left as report-only backlog (not auto-applied), per skill scope rules:**
everything requiring new scripts (`status.mjs`, plate-layout rasterizer,
component-triple-counter, `waitForEngineReady` helper, one-shot bake
wrapper) or hook changes (Stop-hook suppression during in-flight background
agents) — these need building, not an instruction diff.
