# Retro Report — 2026-W34

**Entries reviewed:** 12 (12 distinct sessions/PRs — one entry, `2026-07-20-catalog-refresh-agent-feedback.md`, is itself a facilitator's aggregation of 16 sub-agent retros from PR #929, counted here as one vote per distinct sub-agent theme where noted)  ·  **Span:** 2026-07-03 → 2026-07-20

The inbox hadn't been reviewed since `2026-W27` (last archived entry: 2026-06-28). This batch splits cleanly into two clusters: general app-feature work (assembly view, tool-history repair, Blender-parity verbs + catalog refresh, 3MF packing) and the inverse-CAD reconstruction subsystem (`scripts/inverse-cad/`), which carries its own self-updating `PLAYBOOK.md` and is largely out of this report's CLAUDE.md/docs scope.

## What's working (preserve these)

- **Pure-module-first splits with node unit tests, before touching DOM/Three.js/Worker wiring** — credited independently in the assembly-view, part-grouping, and 3MF-packing entries for letting the hardest logic (grid packing, param union, threaded tree, shelf packing) land fully verified before the risky integration work. Already CLAUDE.md doctrine; still paying off.
- **Reading the tests before writing code** (3MF packing) and **reading the whole recovery surface before touching anything** (tool-history repair) — both turned a scary-looking bug/refactor into a confident, narrowly-scoped fix on the first attempt.
- **The PLAYBOOK ratchet, inverse-CAD's own accumulating-knowledge mechanism** — ratcheting each converged part's tactics into a shared doc before launching the next wave is credited across all five inverse-CAD entries as the reason 21 (then 37, then a never-seen internet mesh) parts converged in ~1 turn median. This is the same principle as this retro-review process itself (cross-agent frequency as signal) applied inside one subsystem — worth recognizing as validation of the pattern, not a new instruction.
- **`work-reviewer` catching real regressions e2e missed** (part-grouping: collapsed-group drag no-op) and **`model:preview`/screenshot-driven visual verification catching defects typecheck can't** (Blender-parity's corduroy-terracing bug) — both already-documented practices, both decisive again this batch.
- **Measure-the-instrument-first** (inverse-CAD v2): a 5-line self-distance check exposed a 0.15mm metric noise floor before trusting any convergence number. Same family as the existing `evaluate()`/SDF-probe doctrine in CLAUDE.md.

## Top friction (ranked by cross-session frequency × cost)

1. **[process] The `Grep` tool has no `-a` override, so it silently misses `main.ts` — CLAUDE.md's existing NUL-byte warning doesn't say this** — 1 session this batch (3MF packing), the **4th** independent session overall per CLAUDE.md's own running count. **Action:** tightened the wording to say explicitly that the dedicated `Grep` tool has no escape hatch at all — go straight to Bash `grep -an`/`rg -a` — instead of leaving an agent to discover that by trying `-a` on the wrong tool first.
2. **[testing] Drag-and-drop e2e against a short scrollable list silently misses rows below the fold** — 1 session (part-grouping), but a direct, cheap, concrete addition to an existing "viewport size" gotcha category already in `docs/playwright-guide.md`. **Action:** added a numbered item naming the failure mode and the fix (bump viewport height for rail/list drag specs).
3. **[tooling, no CLAUDE.md action] A warm dev-server/browser loop for UI iteration** — 2 independent sessions this batch (assembly-view, 3MF-packing), each citing minutes of dead wall-clock per look from cold Playwright WASM-boot + session-build cycles, on top of many prior retro cycles raising the same wish for model-authoring work (already solved there via the `model:preview`/daemon path). No UI-iteration equivalent exists. Left as **backlog** — real infrastructure, not a doc fix.
4. **[tooling] No single trustworthy component/part counter** — engine `decompose()`, the app's `statsComputation` count, and a welded-mesh census disagree on degenerate meshes and `decompose()` itself flip-flops run to run (multipart-kits-overview). Distinct from the already-documented voxel `componentCount`-vs-`voxelPieceCount` trap — this is non-voxel `decompose()` disagreement. **Backlog**: a headless "count it three ways and explain disagreements" tool.
5. **[figures/deform] `scatter` placement feedback (no `placedCount`, no dry-run)** — quadruple-voted this batch alone (4 of 16 catalog-refresh sub-agents independently), on top of already being `#928`'s top item from a prior retro. Reinforcing an existing backlog item rather than a new one — see below.
6. **[process, already resolved] `promptlog-guard` "fires too early" on a compound commit** — raised again in part-grouping, but the fix (stage the log in its own step before the compound commit) already landed in `CLAUDE.md` via the `2026-W27` retro. This session likely predates that fix reaching it; no further action.
7. **[export/packing] No visual/render path for a packed build plate** — 2 independent sessions on 2 different PRs (3MF-packing `#910`, Bambu plate-layout `#904`), both verifying packing purely by parsing `<item>` transforms and asserting bed-bounds numerically, both explicitly wishing for a `model:preview`-equivalent "rasterize the plate arrangement to a PNG." Same root motivation as the existing headless-preview tooling, just for packing instead of geometry. **Backlog.**
8. **[ai chat] Tool-history repair has four independent implementations (Anthropic/OpenAI/Gemini/UI-detector) that can disagree** — already filed as `#914` by the originating session; no further CLAUDE.md action, noted as tracked.

## Already resolved (validated, no action needed)

- **`model:preview --json` needing `--silent`** — flagged again in the catalog-refresh batch, but the fix is already in `CLAUDE.md` (added in an earlier cycle) and the entry itself notes "CLAUDE.md updated this batch."
- **`deform.md`'s "round first, label/paint after" rule, `api.paint.slab`'s one-sided band, and `circularPattern`'s origin-relative radius** — all cited as decisive/fixed by the catalog-refresh batch and confirmed present in `public/ai/deform.md`. These were applied by the source PR itself, not this retro.
- **`promptlog-guard` sequencing** — see friction item 6 above; already documented.

## Changes applied in this PR

- `CLAUDE.md` (`Editing src/main.ts — NUL-byte zones`): clarified that the dedicated `Grep` tool has no `-a` escape hatch at all (unlike raw `grep`/`rg`), so the move is straight to Bash — not a `-a` retry on the wrong tool. Bumped the "N independent sessions" count from three to four.
- `docs/playwright-guide.md`: added item 6 — short scrollable lists (e.g. the parts-list rail) clip drag targets below the fold at the default viewport; bump viewport height for any spec dragging within a scrollable list.

## Backlog (needs a human or the tooling/linter agent)

- [ ] **Warm-browser UI iteration loop** (raised again this batch by 2 sessions, recurring across multiple prior retro cycles): open the editor once, keep WASM hot, drive `window.partwright` + screenshot per iteration — the UI-feature analogue of the existing `bin/partwright.mjs daemon` render path for models.
- [ ] **Headless plate-layout visualizer** (2 sessions, PRs #910 and #904 independently): rasterize a packed build-plate's part footprints to a PNG, the packing equivalent of `model:preview`, so layout bugs are glanceable instead of XML-transform assertions.
- [ ] **Multi-method component-count reconciler**: a headless tool that runs `decompose()`, `statsComputation`, and a welded-mesh census together and explains disagreements on degenerate meshes (multipart-kits-overview session; ~8 bakes → 2 in that session's own estimate).
- [ ] **`scatter` placement feedback** (`placedCount` stat, dry-run anchor-point mode, documented overshoot heuristic) — already `#928`'s top item; this batch's 4-of-16 catalog-refresh sub-agents is a fifth-plus independent vote, worth prioritizing.
- [ ] **Region-scoped `smoothWeld`/`round`** (a `region`/bbox option, or the documented "clip to a tight box around the seam, weld, stitch remainders" recipe promoted to a first-class API) — 2 catalog-refresh sub-agents.
- [ ] **Surface-exposure stat for labels** — a "fraction of label surface exposed" number so a buried-but-nonzero-`triangleCount` label (the toadstool-spots class of bug) is gate-able headlessly (1 sub-agent, flagged as high-value).
- [ ] **`api.placeOnFace(shape, {face})`** — flat relief placement onto one of a box's six faces without hand-derived rotation matrices (1 sub-agent).
- [ ] **Provider-diagnostics tagging for ambiguous 4xx origin** (tool-history-auto-repair session): record which request builder/endpoint actually produced an error plus a pre-send shape summary, so "OpenAI 400 with an Anthropic-format body" (a custom-provider-proxying-to-Claude case) is unambiguous instead of inferred.
- [ ] **Unified tool-history repair pass** applied once before payload construction for all providers, replacing the current four separate implementations — already tracked as `#914`.
- [ ] **Inverse-CAD orchestration driver** (`status.mjs`, never built per the original design): a status table + auto-refill + completion detection across a pool of ~20 part-convergence agents. Two sessions independently hit the gap it would have closed (a part silently never assigned an agent, caught only by a final manual sweep; and "agent still running" vs. "best/ exists" ambiguity that caused a re-bake from an interim result).
- [ ] **Inverse-CAD gate-change re-validation**: stamp the gates-code version into `best`/metrics so a mid-run gate fix (e.g. a genus-convention change) doesn't leave an already-"passing" stored result silently stale.
- [ ] **Inverse-CAD `probe.mjs` raw-points JSON output** — agents currently fall back to center-rays for cavity r(z) with no documented alternative.
- [ ] **Durable-by-default subagent workspaces**: inverse-CAD converged candidates lived in gitignored `.plans/` on an ephemeral container, rescued only by an explicit end-of-session copy-and-commit — a crash before that point would have lost the work.

## One-offs noted (no action this week)

- Missing `init`→`ready` handshake documentation for new geometry-Worker clients (1 session, assembly-view) — real and costly in the moment, but a one-off; worth a documentation pass once a second session hits the same gap rather than writing it from a single paraphrase.
- `src/main.ts` module-scope vs. setup-function-scope boundary undocumented (1 session, assembly-view) — same reasoning; noted, not acted on.
- Cross-tab semantic collision on the same UI glyph (`▦`) between two concurrently-developed features, resolved by differentiating rather than a process fix (1 session).
- `api.text` not working headlessly (font preload gap) — already fixed within the same PR that reported it (Blender-parity verbs).
- Stop-hook firing repeatedly during long inverse-CAD agent waits with nothing to reconcile (1 session) — a possible hook tuning ask, below the systemic bar this week.
- Rate-limit-resilient background agents / crash-resume via on-disk state files — worked as designed when tested (1 session), no gap to close.
- CC BY-ND licensing decisions needing to surface at bake time rather than release time (1 session, foreign-mesh work) — real but narrow; note for whoever next imports third-party mesh corpora.
