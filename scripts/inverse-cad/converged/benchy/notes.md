# benchy — session notes (2026-07-05)

## Verdict: CONVERGED (first foreign mesh through the v2 loop)

Best = attempt 5: **score 0.0984**, 6/6 MUST + 2/2 SHOULD, chamfer 0.0082mm,
hausdorff max 0.1623mm (P99 0.0604), IoU 0.9957, volume ratio 0.999,
area ratio 1.0178, genus 5/5, components 1/1, worst finding 1.9mm³ (< half
the 4mm³ gate). 6 attempts total (1 safety bank + 5 authored/refined).
Best candidate: `best/candidate.js` — 265-section levelSet SDF interpolation,
pitch 0.2 / dp 0.02 / edge 0.15, generated by scratchpad `genbenchy.mjs`
(genfoot armor recipe + benchy ledges + two new SDF tricks, below).

## What the part is

3DBenchy tugboat, 60.0 × 31.0 × 48.0mm, Z-flat, 225,706 tris, volume
15550.5mm³. Watertight but SELF-TOUCHING (502 non-manifold edges, mesh χ
garbage → meshGenus −137.5); **voxel-solid genus 5** (§5.37) is what the
engine-built candidate reports, and gating against it worked exactly as
designed. Bow at +x, stern at −x, chimney at (−4, 0) on the cabin roof.

## The 5 handles (all measured, not folklore)

1–2. **Hawsepipe**: a y-axis tunnel ~2.5mm dia centered (x≈23.25, z≈20.5)
   piercing BOTH bow bulwark walls; the space between them is open air above
   the fore deck (z=15.5), so it is 2 handles, not 1. (Localized by a
   y-ray grid: columns with zero hits = the bore.)
3–5. **Cabin interior with 4 mouths**: arched rear doorway (rear wall
   x −7.3..−2.1), front windshield opening (leaning frame wall x 5.8..11.9),
   and two aligned side windows (x −2..+5, z 20..31.5, one per side wall):
   4 openings into one cavity = 3 handles.

NOT handles: chimney bore (blind — floor at z=37.000, cabin ceiling at
36.61, 0.39mm septum); stern deck pocket (r≈1 at x≈−26.35, open from above,
blind floor at z=6.228); cargo hold (open-top box, interior x −16..−9);
bottom lettering recess (z 0..0.30, opens downward at the build plate).

## Measured z-ledge list (0.01mm-localized; drives the straddle pairs)

0.300 (lettering floor) | 4.520 (hull cavity floor, hole birth) | 6.500
(second under-deck void floor, ΔA −55) | 8.500 (main deck, ΔA −449) |
15.500 (fore deck, ΔA −74) | 20.500 (windshield sill, ΔA −24) | 24.610
(rail step, ΔA −23) | 31.730 / 31.930 (rail-end births) | 34.110
(roof-overhang bottom + door/window arch tops, ΔA +22) | 36.610 (cabin
ceiling, ΔA +213) | 37.000 (chimney bore floor) | 38.860 (roof crown split,
outers 1→2) | 40.280 (roof piece end). The z 10.64–10.82 and 38.7–40.3
falls are CURVED (consistent small ΔA runs — §5.13a rule: not snapped).

## Attempt history / what fixed what

0. Stock genLevelSet (step 0.2/edge 0.2): 2/6 MUST, score 4.36. Two
   foreign-mesh failures: (a) **banded-fallback nesting bug** — at z
   8.7–10.9 the cabin is an OUTER contour nested inside the cockpit HOLE
   (depth-3); emitBandedSegment's `outers.add(...).subtract(all holes)`
   erased the cabin entirely (195mm³ missing); (b) genus 6/5 and deck-plane
   smear at the 8.5 ledge.
1. Full SDF blend of all 265 sections (even-odd sdf2d handles depth-3
   nesting for free) + straddle pairs at 14 measured ledges + §5.25d clamp:
   5/6 MUST + 2/2, score 0.197. Genus 5 verified headlessly BEFORE the turn
   (multi-blob z-runs did NOT destabilize the blend — CAD-smooth slices are
   nothing like the organic-hand instability).
2. **Ledge-exact override + grid de-phase** (see PLAYBOOK candidates):
   6/6 + 2/2, score 0.145 — DONE at attempt 2.
3–5. Polish: wall-exact override variant (0.132), blend-composite override
   (0.131), levelSet edge 0.2→0.15 (0.098; halved the residual MC-lerp
   skins). Remaining largest finding: 1.9mm³ thin-skin on the vertical wall
   x≈−7.27 (cabin rear wall face) — XY-resolution-bound, not structural.

## Optimizer

Not applicable: the candidate has no free scalars — every number is a
measured section/ledge. Nothing to tune; `api.params` never declared.

## What I'd try next (if reopened)

- Nothing needed for gates. For margin: edge 0.12 (cost ~2× attempt 5).
- For CAD-pure code: the superstructure is decodable (chimney is an exact
  circle fan cx=−4, cy=0, r 2.98→3.50 with r≈1.46 bore; walls are planes/
  arcs), but the hull + sheerline must stay levelSet; a hybrid was judged
  not worth the genus risk with all gates already green.

## Strategies tried

See state.json `strategiesTried`. Key negative result: genLevelSet's banded
fallback must not be used on slices with outer-inside-hole nesting until the
emitter composes contours by nesting depth.
